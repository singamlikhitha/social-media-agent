version: "3.8"

services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./social_media_agent.db:/app/social_media_agent.db
    restart: unless-stopped
